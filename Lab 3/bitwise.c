// Xor, Not and And operations

#include <stdio.h>

int main() {
    int a = 10, b = 20, c = 40, d = 30;

    printf("a ^ b: %d\n", a ^ b);
    printf("~a: %d\n", ~a);
    printf("a & b: %d\n", a & b);

    return 0;
}